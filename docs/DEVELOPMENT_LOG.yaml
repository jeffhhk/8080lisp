- meta: {_: feature, guid: 9x2k}
  title: Add separate hello world gcc and cosmocc targets
  prompt: |
    Can you compile a hello world binary using ~/bin/cosmo/bin/cosmocc?  If not, explain what specific access you need.

    Validate with gcc in the path and provide a makefile target for me to run that directs the same thing at cosmocc.
    make clean to remove your cosmo dependencies.  Update IMPLEMENTATION_PLAN to separate cosmocc target from gcc target.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: 1 target built with gcc
    - verification: make test
      result: 1 test command succeeded and printed hello world
    - verification: make -n hello-cosmo
      result: emitted /home/jeff/bin/cosmo/bin/cosmocc -O2 -Wall -Wextra -o o/hello_cosmo.com src/hello_world.c
    - verification: make -n build
      result: emitted gcc -O2 -Wall -Wextra -o o/hello_gcc src/hello_world.c
    - verification: make -n test
      result: emitted gcc build command followed by ./o/hello_gcc
    - verification: size --format=GNU --radix=10 o/hello_gcc
      result: text=369 data=1599 bss=8 total=1976
- meta: {_: feature, guid: h7q2}
  title: Add a GAS runtime scaffold with monitor I/O shims
  prompt: |
    Read orig/lisp_8080_rawocr_2026-04-13.asm
    Assemble, fix and test a gas/gcc binary.
    Provide implementations of:
        - OUTC: output character
        - CRLF: output EOL (LF only)
        - INCH: input character
    Read docs/IMPLEMENTATION_PLAN.md.
    Use the original registers according to the map.
    Have (but do not run) an additional build target that directs the build to the toolchain in ~/bin/cosmo/bin/cosmocc to produce a cosmopolitan binary.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc from GAS and C scaffold sources
    - verification: make test
      result: 2 tests 0 failures 0 skipped
    - verification: inject an artificial failure into src/lisp_runtime_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from src/lisp_runtime_test.c and rerun make test
      result: 2 tests 0 failures 0 skipped
    - verification: make -n lisp-cosmo
      result: emitted /home/jeff/bin/cosmo/bin/cosmocc -O2 -Wall -Wextra -o o/lisp_cosmo.com src/lisp_runtime.S src/lisp_host_io.c src/lisp_main.c
    - verification: size --format=GNU --radix=10 o/lisp_gcc
      result: text=934 data=1968 bss=69712 total=72614
- meta: {_: refactor, guid: r3m8}
  title: Align source layout with the updated implementation plan
  prompt: |
    docs/IMPLEMENTATION_PLAN.md has been updated.  Bring the current project in line with the current version.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc from src/lisp_runtime.S and tests C harness sources
    - verification: make test
      result: 2 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/lisp_runtime_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/lisp_runtime_test.c and rerun make test
      result: 2 tests 0 failures 0 skipped
    - verification: make -n lisp-cosmo
      result: emitted /home/jeff/bin/cosmo/bin/cosmocc -O2 -Wall -Wextra -Isrc -Itests -o o/lisp_cosmo.com src/lisp_runtime.S tests/lisp_host_io.c tests/lisp_main.c
    - verification: size --format=GNU --radix=10 o/lisp_gcc
      result: text=934 data=1968 bss=69712 total=72614
- meta: {_: feature, guid: a4k7}
  title: Add an 8080 assembler that ingests the OCR listing
  prompt: |
    Make me a 8080 emulator.
    Patch the INCH and COUT functions to the host machine so we can see what's going on.
    Make a matching assembler.
    Both the emulator and assembler should be capable of ingesting orig/lisp_8080_rawocr_2026-04-13.asm.
  verifications:
    - verification: make build
      result: built o/lisp_gcc and o/i8080asm with gcc
    - verification: make test
      result: 4 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_asm_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_asm_test.c and rerun make test
      result: 4 tests 0 failures 0 skipped
    - verification: ./o/i8080asm orig/lisp_8080_rawocr_2026-04-13.asm /tmp/lisp.bin && wc -c /tmp/lisp.bin
      result: emitted a 1759-byte flat binary from the OCR listing
- meta: {_: feature, guid: m8t2}
  title: Add an 8080 emulator with host monitor hooks
  prompt: |
    Make me a 8080 emulator.
    Patch the INCH and COUT functions to the host machine so we can see what's going on.
    Make a matching assembler.
    Both the emulator and assembler should be capable of ingesting orig/lisp_8080_rawocr_2026-04-13.asm.
  verifications:
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, and o/i8080emu with gcc
    - verification: make test
      result: 6 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 6 tests 0 failures 0 skipped
    - verification: printf 'ATOM (T) \\n' | ./o/i8080emu orig/lisp_8080_rawocr_2026-04-13.asm
      result: invoked the emulator against the original OCR image with host monitor hooks active
- meta: {_: feature, guid: c7v1}
  title: Add instruction-pointer coverage reporting to the 8080 emulator
  prompt: |
    Modify the emulator to allow collection of a machine instruction by instruction coverage report.  Count how many times each address became the instruction pointer IP.
  verifications:
    - verification: make build
      result: built o/i8080emu with coverage reporting support
    - verification: make test
      result: 7 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 7 tests 0 failures 0 skipped
    - verification: ./o/i8080emu --coverage /tmp/i8080_coverage_fixture.asm
      result: emitted 0x0000 1 with covered_addresses 1 and total_instruction_fetches 1
- meta: {_: feature, guid: q4n8}
  title: Add an OCR discrepancy validator for raw and corrected listings
  prompt: |
    Add a validator tool:

      Read AGENTS.md and docs/IMPLEMENTATION_PLAN.md first.

      Keep orig/lisp_8080_rawocr_2026-04-13.asm unchanged. Add infrastructure for OCR discrepancy
      traceability without correcting the listing yet.

      Implement a tool that ingests:

      - orig/lisp_8080_rawocr_2026-04-13.asm as the raw listing
      - src/lisp_8080_corrected.asm as the corrected listing
      - docs/OCR_CORRECTIONS.yaml as the provenance ledger

      The tool must validate that every field-level discrepancy between the raw and corrected listings is
      accounted for in the provenance ledger, and that every provenance entry corresponds to an actual
      discrepancy.

      Support at least these provenance fields:

      - id
      - line_number
      - address
      - field with values such as bytes, label, mnemonic, operand, comment, whole_line
      - original
      - corrected
      - evidence
      - basis
      - confidence

      Requirements:

      - the validator must compare raw vs corrected listing at field granularity, not just whole-line text
      - the validator must fail on unaccounted discrepancies
      - the validator must fail on stale provenance entries that do not match any discrepancy
      - add automated tests for the validator
      - do not introduce any OCR corrections in this change beyond the minimum fixture data needed to test
        the tool
      - if needed, create placeholder src/lisp_8080_corrected.asm and docs/OCR_CORRECTIONS.yaml with
        minimal content suitable for testing

      Follow all repo policies.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 11 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/ocr_validator_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/ocr_validator_test.c and rerun make test
      result: 11 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator
      result: validated orig/lisp_8080_rawocr_2026-04-13.asm against src/lisp_8080_corrected.asm and docs/OCR_CORRECTIONS.yaml
- meta: {_: feature, guid: p7r4}
  title: Add interpreter-focused Lisp validation tests
  prompt: |
    Add Lisp-interpreter tests:

      Read AGENTS.md and docs/IMPLEMENTATION_PLAN.md first.

      According to the "Testing Strategy" in docs/IMPLEMENTATION_PLAN.md, add a suite of tests for the Lisp
      interpreter itself, focusing on validated behavior of the original Lisp system rather than OCR
      correction infrastructure.

      Requirements:

      - test Lisp interpreter behavior, not just assembler or emulator mechanics
      - cover the Lisp primitives and major code paths required by the implementation plan
      - prefer automated tests
      - reuse existing emulator and host I/O infrastructure where practical
      - leverage the --coverage functionality of the emulator if applicable
      - document validated behavior in docs/Reference.md
      - do not modify orig/lisp_8080_rawocr_2026-04-13.asm in this change
      - do not add OCR provenance or corrected-listing work in this change unless absolutely required for
        test fixtures

      Follow all repo policies.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 16 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/lisp_interpreter_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/lisp_interpreter_test.c and rerun make test
      result: 16 tests 0 failures 0 skipped
- meta: {_: refactor, guid: t5w1}
  title: Convert the implementation backlog to structured YAML
  prompt: |
    I have updated AGENTS.md.  Please convert docs/IMPLEMENTATION_BACKLOG.md to docs/IMPLEMENTATION_BACKLOG.yaml.
  verifications:
    - verification: make test
      result: 16 tests 0 failures 0 skipped
- meta: {_: refactor, guid: u6x2}
  title: Record recent interpreter bugs in the implementation backlog
  prompt: |
    Add to IMPLEMENTATION_BACKLOG.yaml any bugs that you still remember from recent commit 60dabf.
  verifications:
    - verification: make build
      result: build target was already up to date
    - verification: make test
      result: 16 tests 0 failures 0 skipped
- meta: {_: bug, guid: z8p4}
  title: Corrected OCR listing should align opening routine entries and provenance
  prompt: |
    Correct the OCR-derived listing and define and populate provenance data

      Read AGENTS.md and docs/IMPLEMENTATION_PLAN.md first.

      Keep orig/lisp_8080_rawocr_2026-04-13.asm unchanged as the raw OCR source. Create and maintain a
      corrected copy at src/lisp_8080_corrected.asm.

      Using the validator tool and the Lisp/emulator test infrastructure already in the repo, correct
      transcription errors in the OCR-derived listing and create a provenance ledger at docs/
      OCR_CORRECTIONS.yaml.

      Requirements:

      - allow for OCR errors in either the machine-code columns or the assembly text
      - record every correction in docs/OCR_CORRECTIONS.yaml
      - use provenance entries with fields:
          - id
          - line_number
          - address
          - field
          - original
          - corrected
          - evidence
          - basis
          - confidence
      - acceptable basis values may include:
          - instruction-encoding
          - cross-reference
          - control-flow
          - duplicate-pattern
          - runtime-behavior
          - scan-review
      - make only justified corrections
      - keep edits to src/lisp_8080_corrected.asm minimal and traceable
      - run the validator so that all discrepancies between raw and corrected listings are accounted for in
        the provenance ledger
      - extend docs/Reference.md if newly validated behavior becomes clear from this work

      Follow all repo policies.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 18 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/ocr_validator_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/ocr_validator_test.c and rerun make test
      result: 18 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/lisp_interpreter_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/lisp_interpreter_test.c and rerun make test
      result: 18 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator
      result: validated orig/lisp_8080_rawocr_2026-04-13.asm against src/lisp_8080_corrected.asm and docs/OCR_CORRECTIONS.yaml
- meta: {_: bug, guid: m7c4}
  title: OCR provenance should ignore address-only line changes
  prompt: |
    In the provenance report, do not consider a line difference if only the address of the line has changed.  Do consider it different if the machine code for the line is different.
  verifications:
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 20 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/ocr_validator_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/ocr_validator_test.c and rerun make test
      result: 20 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator
      result: validated orig/lisp_8080_rawocr_2026-04-13.asm against src/lisp_8080_corrected.asm and docs/OCR_CORRECTIONS.yaml
- meta: {_: bug, guid: b6r2}
  title: OCR provenance should allow block entries for shifted byte-column runs
  prompt: |
    Add block-level OCR entries for shifted byte-column runs
  verifications:
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 20 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/ocr_validator_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/ocr_validator_test.c and rerun make test
      result: 20 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator
      result: validated orig/lisp_8080_rawocr_2026-04-13.asm against src/lisp_8080_corrected.asm and docs/OCR_CORRECTIONS.yaml
- meta: {_: bug, guid: r3k9}
  title: Reference should match the validated ASSOC lookup path
  prompt: |
    Amend test results to resolve any final issues

      Read AGENTS.md and docs/IMPLEMENTATION_PLAN.md first.

      Review the current repository after the validator, Lisp-interpreter tests, and OCR-correction/
      provenance changes have landed. Resolve any remaining failures, inconsistencies, or validation gaps
      needed to bring the repo to a clean final state.

      Requirements:

      - run the official build and test commands from docs/IMPLEMENTATION_PLAN.md
      - run the OCR discrepancy validator against:
          - orig/lisp_8080_rawocr_2026-04-13.asm
          - src/lisp_8080_corrected.asm
          - docs/OCR_CORRECTIONS.yaml
      - fix any final issues causing failures or contradictions between:
          - tests
          - corrected listing
          - provenance data
          - reference documentation
      - prefer minimal bugfixes over broad rewrites
      - if a failing test was significantly changed, perform the Test Double Check Procedure from AGENTS.md
      - do not introduce unrelated feature work
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 20 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator orig/lisp_8080_rawocr_2026-04-13.asm src/lisp_8080_corrected.asm docs/OCR_CORRECTIONS.yaml
      result: validated the raw listing, corrected listing, and provenance ledger with explicit paths
- meta: {_: feature, guid: v8q1}
  title: Add NDJSON coverage file output to the emulator CLI
  prompt: |
    Write a cli option --coverage-out:
        printf 'EQ (T T)\n' | timeout 2s ./o/i8080emu --coverage-out foo.ndjson src/lisp_8080_corrected.asm

    so that foo.ndjson contains a coverage report.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 21 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 21 tests 0 failures 0 skipped
    - verification: printf 'ORG 0000H\nHLT\n' > /tmp/i8080_cov_check.asm && ./o/i8080emu --coverage-out /tmp/i8080_cov_check.ndjson /tmp/i8080_cov_check.asm
      result: wrote an NDJSON coverage report with address and summary records
- meta: {_: bug, guid: j4n2}
  title: Stdin-driven emulator CLI should print fresh atom results and stop at EOF
  prompt: |
    Please give a command line invocation which can evaluate the constant 3, by piping a suitable input in through stdin, as in "Emulator examples" in Reference.md.
    Please fix any bugs needed to make it work.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 22 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 22 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator orig/lisp_8080_rawocr_2026-04-13.asm src/lisp_8080_corrected.asm docs/OCR_CORRECTIONS.yaml
      result: validated the raw listing, corrected listing, and provenance ledger with explicit paths
    - verification: printf '(LAMBDA (X) X) (3) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
      result: printed >>3 and exited successfully after stdin EOF
- meta: {_: bug, guid: m7q4}
  title: Monitor built-ins should resolve non-head ASSOC bindings
  prompt: |
    Correct and fix the behavior of asking if NIL is NULL:
        printf 'NULL (NIL) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
        printf 'NULL ((NIL)) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
    Whichever version is correct.  Currently both versions print "ABEND".
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 23 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 23 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/lisp_interpreter_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/lisp_interpreter_test.c and rerun make test
      result: 23 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator orig/lisp_8080_rawocr_2026-04-13.asm src/lisp_8080_corrected.asm docs/OCR_CORRECTIONS.yaml
      result: validated the raw listing, corrected listing, and provenance ledger with explicit paths
    - verification: printf 'NULL (NIL) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
      result: printed >>T without ABEND
    - verification: printf 'NULL ((NIL)) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
      result: printed >>F without ABEND
- meta: {_: feature, guid: s4k8}
  title: Add compressed covered-source display to the emulator CLI
  prompt: |
    Write a source displayer for coverage report information and an assembled 8080 program.
    Display only the lines that got coverage, and "..." to compress all the lines in between.
    When the following command line flag is passed:

        printf 'NULL (NIL) \n' | ./o/i8080emu --coverage-source-display-on-exit src/lisp_8080_corrected.asm

    - Collect a coverage report.
    - On exit, invoke the source displayer with the coverage.
    - Preface the source display with a newline to separate the evaluation response from the coverage report.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 24 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 24 tests 0 failures 0 skipped
    - verification: printf 'NULL (NIL) \n' | ./o/i8080emu --coverage-source-display-on-exit src/lisp_8080_corrected.asm
      result: printed >>T, then a separating newline, then a compressed source coverage report with covered lines and ellipsis gaps
- meta: {_: feature, guid: t5m1}
  title: Add baseline subtraction for emulator source coverage display
  prompt: |
    Implement --coverage-baseline baseline.ndjson:
        printf 'CAR ((T.F)) \n' | timeout 2s ./o/i8080emu --coverage-baseline baseline.ndjson --coverage-source-display-on-exit src/lisp_8080_corrected.asm

    When a baseline is provided, before displaying the final source run, subtract off the baseline coverage as "not interesting".  Only display the lines that are in excess of the baseline.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 25 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 25 tests 0 failures 0 skipped
    - verification: printf 'CAR ((T.F)) \n' | ./o/i8080emu --coverage-out /tmp/i8080_cov_baseline.ndjson src/lisp_8080_corrected.asm
      result: wrote a baseline NDJSON file for the monitor query, while also revealing that the current `CAR ((T.F))` evaluator output is corrupted and should be tracked separately
    - verification: printf 'CAR ((T.F)) \n' | timeout 2s ./o/i8080emu --coverage-baseline /tmp/i8080_cov_baseline.ndjson --coverage-source-display-on-exit src/lisp_8080_corrected.asm
      result: subtracted the identical baseline and printed only the evaluation result plus an empty `source coverage:` section with no residual covered lines
- meta: {_: feature, guid: u6n4}
  title: Add boolean baseline filtering for emulator source coverage display
  prompt: |
    Implement --coverage-baseline-boolean baseline.ndjson:
        printf 'CAR ((T.F)) \n' | timeout 2s ./o/i8080emu --coverage-baseline-boolean baseline.ndjson --coverage-source-display-on-exit src/lisp_8080_corrected.asm

    When a baseline is provided, before displaying the final source run, remove from the coverage every line in the baseline as "not interesting".  Then display the remaining lines/counts.
    Retain the existing --coverage-baseline feature.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 26 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 26 tests 0 failures 0 skipped
    - verification: printf 'CAR ((T.F)) \n' | ./o/i8080emu --coverage-out /tmp/i8080_cov_boolean_baseline.ndjson src/lisp_8080_corrected.asm
      result: wrote a baseline NDJSON file for the monitor query
    - verification: printf 'CAR ((T.F)) \n' | timeout 2s ./o/i8080emu --coverage-baseline-boolean /tmp/i8080_cov_boolean_baseline.ndjson --coverage-source-display-on-exit src/lisp_8080_corrected.asm
      result: printed the evaluation result plus an empty `source coverage:` section because every covered source line in the run was touched by the baseline
- meta: {_: refactor, guid: n7c4}
  title: Restructure reference documentation around the current documentation strategy
  prompt: |
    The "Documentation strategy" has changed in IMPLEMENTATION_PLAN.md.  Bring the documentation up to date to reflect the strategy.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 26 tests 0 failures 0 skipped
    - verification: ./o/ocr_validator
      result: validated the default raw listing, corrected listing, and provenance ledger
    - verification: printf '(LAMBDA (X) X) (3) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
      result: printed >>3 and exited successfully after stdin EOF
    - verification: printf 'NULL (NIL) \n' | ./o/i8080emu --coverage-source-display-on-exit src/lisp_8080_corrected.asm
      result: printed >>T followed by a newline-prefixed covered-source report
- meta: {_: bug, guid: q9d6}
  title: Coverage source display should omit leading source line numbers
  prompt: |
    Remove the leading line numbers from the --coverage-source-display-on-exit.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 26 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make test
      result: make test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_emu_test.c and rerun make test
      result: 26 tests 0 failures 0 skipped
    - verification: printf 'NULL (NIL) \n' | ./o/i8080emu --coverage-source-display-on-exit src/lisp_8080_corrected.asm | sed -n '1,12p'
      result: printed source coverage lines that begin with hit totals and source text, without a leading source line number column
- meta: {_: refactor, guid: b4k2}
  title: Route cosmocc builds through the shared recursive build target
  prompt: |
    Refactor the Makefile so that one "build-cosmo" target builds the whole project under cosmocc.

    Use the "Recursive make with variable overrides" style.
    Invoke "make clean" before recursing to build.
    To keep it simple, reuse all target names between cosmo and noncosmo.
    Use the x86_64-unknown-cosmo-cc compiler so that you can assemble lisp_runtime.S.
    If needed, augment the path with the cosmocc directory:
        env PATH=$PATH:$HOME/bin/cosmo/bin
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 26 tests 0 failures 0 skipped
    - verification: PATH="$PATH:$HOME/bin/cosmo/bin" command -v x86_64-unknown-cosmo-cc
      result: resolved /home/jeff/bin/cosmo/bin/x86_64-unknown-cosmo-cc
    - verification: make build-cosmo
      result: ran make clean and rebuilt o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with x86_64-unknown-cosmo-cc
- meta: {_: feature, guid: w3f8}
  title: Emit cross-platform `.com` binaries from the Cosmopolitan build
  prompt: |
    Patch the Makefile so I can build on linux and run the resulting binaries on MacOS and Windows.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make test
      result: 26 tests 0 failures 0 skipped
    - verification: PATH="$PATH:$HOME/bin/cosmo/bin" command -v x86_64-unknown-cosmo-cc
      result: resolved /home/jeff/bin/cosmo/bin/x86_64-unknown-cosmo-cc
    - verification: make build-cosmo
      result: rebuilt o/lisp_gcc.com, o/i8080asm.com, o/i8080emu.com, and o/ocr_validator.com with x86_64-unknown-cosmo-cc and repackaged o/i8080emu.com as a self-extracting archive
    - verification: ls o/*.com
      result: listed o/i8080asm.com, o/i8080emu.com, o/lisp_gcc.com, and o/ocr_validator.com
    - verification: printf 'NULL (NIL) \n' | ./o/i8080emu.com src/lisp_8080_corrected.asm
      result: printed >>T from the Cosmopolitan emulator binary on Linux
    - verification: unzip -l o/i8080emu.com | sed -n '1,12p'
      result: listed the embedded source and documentation payload from the packaged Cosmopolitan emulator binary
- meta: {_: feature, guid: w8k3}
  title: Add a standalone Emscripten web page target for the corrected Lisp monitor
  prompt: |
    Make a secondary target for running
        ./o/i8080emulisp.com src/lisp_8080_corrected.asm
    as a standalone web page.
    Use the following strategy:

        For emscripten compatibility, make a fork of lisp_runtime.S as lisp_runtime_emscripten.c.
        Make and test two standalone scripts ensure_emsdk_installed.sh and  ensure_emsdk_uninstalled.sh.
        Write a separate optional build target i8080web incorporating lisp_runtime_emscripten.c.
        Generate a simple HTML page with a textarea or other terminal widget for input/output.
        Generate an html page that inlines the compiled i8080emu and lisp_8080_corrected.asm, but not the assembler or source code.
        Make any necessary refactorings to reuse as much code as possible without breaking the existing code.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: rebuilt the native gcc tools and left the optional web target separate from the default build
    - verification: make test
      result: 30 tests 0 failures 0 skipped
    - verification: inject an artificial failure into tests/i8080_web_test.c and rerun make o/i8080_web_test && ./o/i8080_web_test
      result: the web smoke test failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_web_test.c and rerun make o/i8080_web_test && ./o/i8080_web_test
      result: 2 tests 0 failures 0 skipped
    - verification: EMSDK_VERSION=latest ./ensure_emsdk_installed.sh /tmp/8080lisp-emsdk-check
      result: installed a temporary emsdk checkout, resolved `latest` to 5.0.7, and reported `emcc` 5.0.7
    - verification: EMSDK_DIR=/tmp/8080lisp-emsdk-check EMSDK_VERSION=latest make i8080web
      result: generated `o/i8080web.html` as a 104K standalone page
    - verification: rg -a -n "Run Monitor Input|textarea|script src=|src/i8080_asm.c|src/lisp_8080_corrected.asm|data:application/octet-stream;base64" o/i8080web.html
      result: confirmed the generated page contains the textarea UI, embeds the corrected Lisp listing, and does not rely on an external script asset or ship `src/i8080_asm.c`
    - verification: ./ensure_emsdk_uninstalled.sh /tmp/8080lisp-emsdk-check
      result: removed the temporary emsdk checkout after the web build verification
- meta: {_: bug, guid: y4p6}
  title: i8080web should keep browser emulator state off the wasm stack
  prompt: |
    Please fix:
    Loading o/i8080web.html in firefox and clicking "Run Monitor Input" shows in the console:
        Uncaught RuntimeError: index out of bounds
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: rebuilt the native gcc tools successfully after moving browser-only emulator state out of the call stack
    - verification: make test
      result: 30 tests 0 failures 0 skipped
    - verification: EMSDK_VERSION=latest ./ensure_emsdk_installed.sh /tmp/8080lisp-emsdk-fix && EMSDK_DIR=/tmp/8080lisp-emsdk-fix EMSDK_VERSION=latest make i8080web && ./ensure_emsdk_uninstalled.sh /tmp/8080lisp-emsdk-fix
      result: installed a temporary emsdk checkout, rebuilt `o/i8080web.html` with the stack fix in place, and removed the temporary checkout afterward
- meta: {_: bug, guid: c7n5}
  title: CAR monitor evaluation should print the compact atom result
  prompt: |
    Resolve PORT-0002L.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: inject an artificial failure into tests/i8080_emu_test.c and rerun make o/i8080_emu_test && ./o/i8080_emu_test
      result: the new CLI regression failed with "car query test double check"
    - verification: inject an artificial failure into tests/lisp_interpreter_test.c and rerun make o/lisp_interpreter_test && ./o/lisp_interpreter_test
      result: the new parser regression failed with "monitor parser test double check"
    - verification: remove the artificial failures from tests/i8080_emu_test.c and tests/lisp_interpreter_test.c, then rerun make test
      result: 32 tests 0 failures 0 skipped
    - verification: printf 'CAR ((T.F)) \n' | ./o/i8080emu src/lisp_8080_corrected.asm
      result: printed >>T and exited successfully after stdin EOF
- meta: {_: feature, guid: e2v9}
  title: Keep the browser monitor session live until explicit restart
  prompt: |
    In make package-web, change the name of the "Run Monitor Input" button to eval, and make it so that the evaluator does not reset afterward.  Then make a separate button to the button's right called "Restart" which resets the emulator and Output window.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: rebuilt o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: inject an artificial failure into tests/i8080_web_test.c and rerun make o/i8080_web_test && ./o/i8080_web_test
      result: the stateful web regression failed with "artificial failure before final success"
    - verification: remove the artificial failure from tests/i8080_web_test.c and rerun make o/i8080_web_test && ./o/i8080_web_test
      result: 4 tests 0 failures 0 skipped
    - verification: make test
      result: 34 tests 0 failures 0 skipped
    - verification: make package-web
      result: rebuilt o/i8080web.html with `eval` and `Restart` controls plus the exported `i8080web_restart` entrypoint
- meta: {_: feature, guid: f4p2}
  title: Deploy the packaged web monitor to GitHub Pages from CI
  prompt: |
    Apply these instructions:
        https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages
    so that when I push to main, the generated o/i8080web.html becomes my project's pages' index.html.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: rebuilt o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 34 tests 0 failures 0 skipped
    - verification: make package-web
      result: rebuilt o/i8080web.html successfully for the same artifact path the Pages workflow stages as `index.html`
    - verification: sed -n '1,220p' .github/workflows/pages.yml
      result: confirmed the workflow triggers on pushes to `main`, runs `make package-web`, stages `github-pages-site/index.html`, uploads the Pages artifact, and deploys it
- meta: {_: bug, guid: g5u1}
  title: GitHub Pages workflow should use current actions and explain first-run enablement
  prompt: |
    I pushed my repository and got the following Github Actions log:     Annotations
        2 errors and 1 warning
        build
        HttpError: Not Found - https://docs.github.com/rest/pages/pages#get-a-apiname-pages-site
        HttpError: /home/runner/work/_actions/actions/configure-pages/v5/node_modules/@octokit/request/dist-node/index.js#L124
        Get Pages site failed. Please verify that the repository has Pages enabled and configured to build using GitHub Actions, or consider exploring the `enablement` parameter for this action. Error: Not Found - https://docs.github.com/rest/pages/pages#get-a-apiname-pages-site
        build
        Node.js 20 actions are deprecated. The following actions are running on Node.js 20 and may not work as expected: actions/checkout@v4, actions/configure-pages@v5. Actions will be forced to run with Node.js 24 by default starting June 2nd, 2026. Node.js 20 will be removed from the runner on September 16th, 2026. Please check if updated versions of these actions are available that support Node.js 24. To opt into Node.js 24 now, set the FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true environment variable on the runner or in your workflow file. Once Node.js 24 becomes the default, you can temporarily opt out by setting ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION=true. For more information see: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: rebuilt o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 34 tests 0 failures 0 skipped
    - verification: sed -n '1,220p' .github/workflows/pages.yml
      result: confirmed the workflow now uses `actions/checkout@v5`, `actions/configure-pages@v6`, and `actions/deploy-pages@v5`
    - verification: make package-web
      result: rebuilt o/i8080web.html successfully after the workflow compatibility update
- meta: {_: feature, guid: c3m7}
  title: Publish the Cosmopolitan self-extracting archive from GitHub Actions
  prompt: |
    Please make Github Actions compile my "make build-cosmo" and publish o/8080LISP.com as an artifact.  (Installation of cosmocc required)
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 34 tests 0 failures 0 skipped
    - verification: make build-cosmo
      result: built the Cosmopolitan targets and packaged o/8080LISP.com
    - verification: sed -n '1,220p' .github/workflows/cosmo.yml
      result: confirmed the workflow installs cosmocc, runs make build-cosmo, and uploads o/8080LISP.com with actions/upload-artifact@v4
- meta: {_: feature, guid: u3r8}
  title: Publish the Cosmopolitan archive to GitHub Releases
  prompt: |
    Add a release workflow that creates or updates a GitHub Release and attaches
    8080LISP.com.
  verifications:
    - verification: make clean
      result: removed build output under o/
    - verification: make build
      result: built o/lisp_gcc, o/i8080asm, o/i8080emu, and o/ocr_validator with gcc
    - verification: make test
      result: 34 tests 0 failures 0 skipped
    - verification: make build-cosmo
      result: built the Cosmopolitan targets and packaged o/8080LISP.com
    - verification: sed -n '1,220p' .github/workflows/release.yml
      result: confirmed the workflow resolves a release tag, installs cosmocc, runs make build-cosmo, creates or updates the matching GitHub Release, and uploads o/8080LISP.com with gh release upload --clobber
